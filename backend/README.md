# Backend Scaffold

Run the local API and SQLite health check:

```bash
cd backend
..\backend\.venv\Scripts\python.exe -m app.main
```

Health endpoints:

- `GET /api/health`
- `GET /api/db/health`
- `GET /api/schedules`
- `GET /api/news/articles`
